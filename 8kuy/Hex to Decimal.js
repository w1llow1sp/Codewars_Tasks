/**
 * Выполните функцию, которая преобразует
 * шестнадцатеричное число (заданное в виде строки) в десятичное.
 */

function hexToDec(hexString){
    return (parseInt(hexString,16))
}

hexToDec("1")
hexToDec("a")
hexToDec("10")
hexToDec("FF")
hexToDec("FF")