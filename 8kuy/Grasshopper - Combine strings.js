/**
 * Функция объединения строк Создайте функцию с именем combineNames/combine_names/CombineNames, принимающую два параметра (имя и фамилию). Функция должна возвращать полное имя. Пример: при использовании "James" в качестве имени и "Stevens" в качестве фамилии функция должна возвращать "James Stevens".
 */

const combineNames = (str1, str2) => `${str1} ${str2}`

//const combineNames = (...names) => names.join(' ');